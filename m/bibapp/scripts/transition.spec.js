define(['jquery', 'transition', 'backbone'], function($, Transition, Backbone) {
    describe('transition', function() {
        /*
        var a = new Backbone.View;
        var b = new Backbone.View;

        beforeEach(function(){
        });

        it('changes from one text to another', function() {
            transition = new Transition(a);
            transition.create({elem: $test, initial: $a});

            waitsFor(function() { 
                    initialised && expect(initialised).toBe(true);
                    return initialised; 
                }, "didn't run script within 10 sec.", 10000);
        });

        */
    });
});
